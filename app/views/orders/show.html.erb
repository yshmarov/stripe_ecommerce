<%= meta_tags(title: t(".title", slug: @order.slug.upcase)) %>

<%= turbo_stream_from @order %>

<%= render @order %>

<details open>
  <summary>
    <%= t("orders.order.items").capitalize %>
  </summary>
  <%= render @order_items %>
</details>
